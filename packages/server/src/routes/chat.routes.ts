import { Router } from "express";

const router = Router();

// TODO: POST /  — SSE chat stream
// TODO: GET /conversations — list conversations
// TODO: GET /conversations/:id/messages — get messages

export default router;
