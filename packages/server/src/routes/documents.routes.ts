import { Router } from "express";

const router = Router();

// TODO: POST /generate — skill-based document generation
// TODO: GET / — list documents
// TODO: GET /:id — get document
// TODO: DELETE /:id — delete document

export default router;
