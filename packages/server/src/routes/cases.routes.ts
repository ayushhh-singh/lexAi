import { Router } from "express";

const router = Router();

// TODO: POST / — create case
// TODO: GET / — list cases
// TODO: GET /:id — get case
// TODO: PATCH /:id — update case
// TODO: DELETE /:id — delete case

export default router;
